/**
 * AssetBulkUpload — the /bulk/assets upload pane.
 *
 * Stub: the file upload, preview, per-line matching (asset / model / client
 * / site / status), and background-job polling flow is built separately —
 * see docs/superpowers/specs/2026-09-24-bulk-update-assets-design.md.
 */
export default function AssetBulkUpload() {
  return <p className="page-hint">The upload step is coming soon.</p>;
}
